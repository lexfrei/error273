[package]
name = "error273"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19", default-features = false, features = ["default_app"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
